using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace SBC.Entities
{
    public class TestCourses
    {
        public static int Test(Object test)
        {
            int tmp = 0;
            int res = 0;
            //foreach (var check in test)
            //{
            //    tmp++; 
            //}
            for (int i = 0; i < tmp; i++)
            {

            }
            return res;
        }
    }
}