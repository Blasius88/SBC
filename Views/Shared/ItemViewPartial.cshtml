@model SBC.DAL.Courses

<div class='col-md-3'>
    <div class='thumbnail'>
        <img height="150" width="150"
             src="@Url.Action("getImage",
                 new
                 {
                     id = Model.id
                 })"
             alt='NoImage'
             class="img-thumbnail" />
        <div class="caption">
            <div class="item-name">
                <h4>@Model.NameCourses</h4>
            </div>
            <div class="item-description">
                @Model.Description
            </div>
            <div class="centered">
                <a href=@Url.Action("Biologia",
                      "Courses")
                   class="btn btn-primary">
                    <span class="glyphicon glyphicon-shoppingcart">
                    </span>
                    Перейти на курс
                </a>
            </div>
        </div>
    </div>
</div>